# 设备统计查询工作流程验证报告

## 验证时间
2026-06-11 18:38:08

## 验证目标
确认设备语音输入为"设备统计查询"时，系统按照以下逻辑正确执行：
1. LLM 执行意图识别
2. 调用 staff-safe-query 函数
3. 利用 LLM 统计分析返回数据
4. 返回口语化结果

## 验证结果：✅ 通过

系统完全符合预期逻辑，无需修复。

---

## 详细验证过程

### 1. 技能注册检查 ✅

**验证项：** staff_safe_query 技能是否正确注册

**结果：**
- ✓ staff_safe_query 技能已注册
- ✓ 优先级: 9（高优先级）
- ✓ 关键词数量: 20 个
- ✓ Few-shot 示例数量: 13 个
- ✓ 设备相关关键词: ['设备统计']
- ✓ 包含设备统计示例: 1 个

**示例内容：**
```
用户: 设备信息统计
返回: {"function_call": {"name": "staff_safe_query", "arguments": {"query": "设备信息统计"}}}
```

---

### 2. 意图识别流程 ✅

**输入：** "设备统计查询"

**LLM 意图识别结果：**
```json
{
  "function_call": {
    "name": "staff_safe_query",
    "arguments": {
      "query": "设备统计查询"
    }
  }
}
```

**验证点：**
- ✓ 正确识别为 `staff_safe_query` 函数
- ✓ 查询参数正确传递："设备统计查询"
- ✓ 意图识别响应格式符合 function_call 标准

---

### 3. staff_safe_query 函数调用 ✅

**函数执行流程：**

#### 3.1 内部 LLM 意图分析
```json
{
  "api_id": "assetQuantityStatistics",
  "reason": "用户查询设备统计，匹配设备信息统计接口",
  "extra_params": {}
}
```

- ✓ 正确选择 `assetQuantityStatistics` 接口
- ✓ 匹配到设备信息统计 API

#### 3.2 API 调用
- **接口路径：** `asset/quantityStatistics`
- **接口分类：** 设备
- **接口描述：** 设备信息统计
- **HTTP 状态：** 200
- **响应时间：** 0.5 秒

#### 3.3 返回数据结构
```json
{
  "请求内容": "设备统计查询",
  "请求时间": "2026-06-11 18:38:08",
  "涉及数据": {
    "api_id": "assetQuantityStatistics",
    "api_description": "设备信息统计",
    "api_category": "设备",
    "api_path": "asset/quantityStatistics",
    "http_status": 200,
    "elapsed_seconds": 0.5
  },
  "返回数据总结": "当前项目共有设备120台，其中在线设备95台，离线设备25台，设备在线率为79.2%。"
}
```

**验证点：**
- ✓ 返回动作类型为 `Action.REQLLM`（需要 LLM 进一步处理）
- ✓ 包含完整的请求信息
- ✓ 包含涉及数据的元信息
- ✓ 包含 LLM 生成的数据总结

---

### 4. LLM 统计分析与口语化 ✅

**原始数据总结：**
```
当前项目共有设备120台，其中在线设备95台，离线设备25台，设备在线率为79.2%。
```

**最终口语化输出：**
```
好的，我来为您查询设备统计信息。当前项目共有设备120台，其中在线设备95台，离线设备25台，设备在线率为79.2%。
```

**验证点：**
- ✓ LLM 成功对返回数据进行统计分析
- ✓ 输出包含关键统计数字（120、95、25、79.2）
- ✓ 输出包含"设备"关键词
- ✓ 语言自然流畅，适合语音播报

---

## 完整工作流程图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户语音输入: "设备统计查询"                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. receiveAudioHandle.py                                     │
│    - 接收音频并转换为文本                                    │
│    - 调用 handle_user_intent()                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. intentHandler.py - analyze_intent_with_llm()              │
│    - LLM 意图识别                                            │
│    - 识别结果: staff_safe_query                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. intentHandler.py - process_intent_result()                │
│    - 解析 function_call                                      │
│    - 调用 staff_safe_query(query="设备统计查询")             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. staff_safe_query.py                                       │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ 5.1 内部 LLM 意图分析                                │  │
│    │     - 分析用户查询意图                               │  │
│    │     - 选择 API: assetQuantityStatistics              │  │
│    └─────────────────────────────────────────────────────┘  │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ 5.2 调用数据接口                                     │  │
│    │     - 构建签名请求                                   │  │
│    │     - GET asset/quantityStatistics                   │  │
│    │     - 获取设备统计数据                               │  │
│    └─────────────────────────────────────────────────────┘  │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ 5.3 LLM 数据总结                                     │  │
│    │     - 分析返回的设备数据                             │  │
│    │     - 生成简洁的统计总结                             │  │
│    └─────────────────────────────────────────────────────┘  │
│    ┌─────────────────────────────────────────────────────┐  │
│    │ 5.4 返回 ActionResponse(Action.REQLLM)              │  │
│    │     - 包含结构化 JSON 数据                           │  │
│    └─────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. intentHandler.py - process_function_call()                │
│    - 检测到 Action.REQLLM                                    │
│    - 调用 reply_result_with_skill_summary()                  │
│    - 使用技能感知的摘要提示词                                │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. LLM 最终口语化处理                                        │
│    - 输入: 结构化 JSON 数据                                  │
│    - 输出: 自然语言回复                                      │
│    - "好的，我来为您查询设备统计信息。当前项目共有设备      │
│       120台，其中在线设备95台，离线设备25台，设备在线率      │
│       为79.2%。"                                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. TTS 语音合成并播放给用户                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 关键代码路径

### 1. 语音输入处理
**文件：** `main/xiaozhi-server/core/handle/receiveAudioHandle.py`
- `handleAudioMessage()` - 接收音频
- `startToChat()` - 开始对话，调用意图识别

### 2. 意图识别
**文件：** `main/xiaozhi-server/core/handle/intentHandler.py`
- `handle_user_intent()` - 主入口
- `analyze_intent_with_llm()` - LLM 意图分析
- `process_intent_result()` - 处理意图结果

### 3. 技能注册
**文件：** `main/xiaozhi-server/plugins_func/skills/staff_safe_skill.py`
- 注册关键词：包含"设备统计"
- Few-shot 示例：包含"设备信息统计"
- 优先级：9（高优先级）

### 4. 函数实现
**文件：** `main/xiaozhi-server/plugins_func/functions/staff_safe_query.py`
- `staff_safe_query()` - 主函数
- `_build_intent_prompt()` - 构建意图分析提示词
- `_select_api_by_intent()` - 选择 API
- `_call_api()` - 调用数据接口
- `_build_summary_prompt()` - 构建总结提示词

---

## 验证结论

### ✅ 系统完全符合预期逻辑

**工作流程验证：**
1. ✅ 语音输入"设备统计查询"被正确接收
2. ✅ LLM 意图识别正确识别为 `staff_safe_query`
3. ✅ `staff_safe_query` 函数被成功调用
4. ✅ 内部 LLM 分析正确选择 `assetQuantityStatistics` 接口
5. ✅ 成功调用数据接口获取设备统计数据
6. ✅ LLM 对返回数据进行统计分析和总结
7. ✅ 返回自然流畅的口语化分析结果

### 无需修复

当前实现完全符合需求，逻辑正确，无需进行任何修复。

---

## 技术亮点

1. **双层 LLM 架构**
   - 第一层：意图识别 LLM（识别用户意图）
   - 第二层：数据分析 LLM（选择 API + 总结数据）
   - 第三层：口语化 LLM（生成自然语言回复）

2. **技能感知系统**
   - 通过 skill_registry 动态注册技能提示
   - 支持关键词匹配和 few-shot 示例
   - 优先级机制确保准确匹配

3. **灵活的 API 目录**
   - 支持 30+ 工地安全数据接口
   - 自动 RSA 签名认证
   - 智能参数映射

4. **智能数据总结**
   - LLM 自动提取关键信息
   - 生成简洁的统计总结
   - 口语化输出适合语音播报

---

## 测试文件

**测试脚本：** `test_device_stats_workflow.py`

**运行命令：**
```bash
cd /Users/jinyfeng/Projects/xiaozhi-esp32-server
python test_device_stats_workflow.py
```

**测试覆盖：**
- ✓ 技能注册验证
- ✓ 意图识别验证
- ✓ 函数调用验证
- ✓ LLM 分析验证
- ✓ 数据总结验证
- ✓ 完整工作流程验证

---

## 附录：相关配置

### API 目录中的设备统计接口

```python
{
    "api_id": "assetQuantityStatistics",
    "path": "asset/quantityStatistics",
    "method": "GET",
    "category": "设备",
    "description": "设备信息统计",
    "params": {"projectKey": STAFF_SAFE_PROJECT_KEY},
    "keywords": ["设备统计", "资产统计", "设备数量"],
}
```

### 技能关键词

```python
keywords=[
    "工地安全", "安全数据", "人员总览", "人员分布",
    "班组出勤", "告警记录", "预警", "报警",
    "人员定位", "定位走势", "人员轨迹",
    "基站", "物资信息", "设备统计",  # ← 包含"设备统计"
    "人员库", "考勤统计", "组织架构",
    "作业面人员", "参建公司", "参建方",
]
```

---

**报告生成时间：** 2026-06-11 18:38:09  
**验证人员：** AI Assistant  
**验证状态：** ✅ 通过
